# ----------------------------------------------------------------------
#
# This makefile has been automatically generated by WinGDB 3.5, build 1857
# Do not edit.
#
# Project: PbTestSuite
#
# ----------------------------------------------------------------------

PLATFORM=unix

ifneq ("$(windir)","")
PLATFORM=windows
endif

ifneq ("$(WinDir)","")
PLATFORM=windows
endif

ifneq ("$(WINDIR)","")
PLATFORM=windows
endif

UNAME=$(shell uname -o)

ifeq ("$(UNAME)","Cygwin")
PLATFORM=cygwin
endif

ifeq ("$(BUILD_CONFIGURATION)","")
	BUILD_CONFIGURATION=Debug
endif

ifeq ("$(BUILD_CONFIGURATION)","Debug")
CC= gcc
CXX= g++
AR= ar
CFLAGS= -g
CXXFLAGS= -g -std=c++0x -I ../cxxtest -I ./ -I ../../ -I ../common -D linux
ARFLAGS= cru
LDFLAGS= -g
RESULT=PbTestSuite
INTERMEDIATE_DIR=Debug/obj
DESTINATION_DIR=Debug/bin
ifeq ("$(PLATFORM)","cygwin")
	RM=rm -f
	MKDIR=mkdir -p
	INTERMEDIATE_DIR_MK=$(INTERMEDIATE_DIR)
	DESTINATION_DIR_MK=$(DESTINATION_DIR)
else
	RM= rm -f
	MKDIR= mkdir -p
	INTERMEDIATE_DIR_MK=Debug/obj
	DESTINATION_DIR_MK=Debug/bin
endif

endif

SOURCES= PbTestSuiteRunner.cpp

SOURCEPATHS= Src/Generated/PbTestSuiteRunner.cpp

HEADERS= TestByteOrder.h TestMeta.h basic_list.h bit_field.h bit_field_list.h byte_order.h compiler.h conditional.h container_size.h dynamic.h integer_sequence.h length.h meta_error.h meta_foreach.h meta_fwd.h meta_macros.h meta_math.h meta_util.h offset_of.h size_at.h size_of.h test_def.h test_helper.h tr1_helper.h type_at.h type_list.h type_node.h

VPATH= $(INTERMEDIATE_DIR):Src/Generated

OBJECTNAMES=$(patsubst %.c,%.o,$(patsubst %.cpp,%.o,$(SOURCES)))
OBJECTPATHS=$(addprefix $(INTERMEDIATE_DIR)/,$(OBJECTNAMES))

$(DESTINATION_DIR)/$(RESULT): $(OBJECTNAMES)
ifeq ("$(wildcard $(DESTINATION_DIR) )","")
	-$(MKDIR) $(DESTINATION_DIR_MK)
endif
	$(CXX) $(SYSROOTFLAGS) $(OBJECTPATHS) -o $@ $(LDFLAGS)

$(INTERMEDIATE_DIR)/PbTestSuite.d: $(SOURCES) $(HEADERS)
ifeq ("$(wildcard $(DESTINATION_DIR) )","")
	-$(MKDIR) $(DESTINATION_DIR_MK)
endif
ifeq ("$(wildcard $(INTERMEDIATE_DIR) )","")
	-$(MKDIR) $(INTERMEDIATE_DIR_MK)
endif
	$(CXX) $(CXXFLAGS) -MM $(SOURCEPATHS) > $@

-include $(INTERMEDIATE_DIR)/PbTestSuite.d

clean:
	$(RM) $(DESTINATION_DIR)/$(RESULT)
	$(RM) $(INTERMEDIATE_DIR)/PbTestSuite.d
	$(RM) $(OBJECTPATHS)

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c -o $(INTERMEDIATE_DIR)/$@ $<

%.o: %.c
	$(CC) $(CFLAGS) -c -o $(INTERMEDIATE_DIR)/$@ $<
